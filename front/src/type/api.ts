// api Data 타입
export type ApiStackData = {
  number: number;
  stack: string;
  title: string;
  htmlContent: string;
  createdAt: string;
};

// 보낼 데이터
export type PostDataType = {
  title: string;
  stack: string;
  htmlContent: string;
};
