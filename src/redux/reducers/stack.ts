import { StackAction } from "../../type/aboutReducer";

const initialStack = {
  stack: "",
};

const stackReducer = (state = initialStack, action) => {
  switch (action.type) {
    case "SET_STACK":
  }
};
